- endpoint: http://localhost:9090
  metrics:
  - metrics.yaml
  indexer:
    type: local
    metricsDirectory: metrics

- endpoint: http://localhost:9090
  metrics:
  - metrics.yaml
  indexer:
      esServers: ["{{.ES_SERVER}}"]
      insecureSkipVerify: true
      defaultIndex: upstream-ovnk-kube-burner
      type: opensearch
